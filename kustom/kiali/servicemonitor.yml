apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kiali
  namespace: istio-system
spec:
  endpoints:
  - interval: 10s
    path: /metrics
    port: http-metrics
  namespaceSelector:
    matchNames:
    - istio-system
  selector:
    matchLabels:
      app: kiali
